# Server-only (Vercel → Project Settings → Environment Variables)
# Never prefix with VITE_ — these must not reach the browser.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional public site URL for meta tags
# VITE_SITE_ORIGIN=https://www.solverwebsite.com
